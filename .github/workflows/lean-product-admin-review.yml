name: Lean Product Admin Review

on:
  pull_request:
    branches:
      - __temporarily_disabled_for_media_optimization__

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporarily disabled while exact approved media derivatives are generated."
