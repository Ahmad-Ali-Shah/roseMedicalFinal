import { RoutePlaceholder } from "@/components/layout/route-placeholder";

export default function NotFound() {
  return <RoutePlaceholder eyebrow="Not found" title="The requested page does not exist." path="404" />;
}
