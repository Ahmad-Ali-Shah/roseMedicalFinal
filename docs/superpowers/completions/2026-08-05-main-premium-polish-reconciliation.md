# Main–Premium Visual Polish Reconciliation Completion

**Date:** 2026-08-05  
**Repository:** `manbtd0-cloud/RosaMedical`  
**Branch:** `integration/main-premium-polish-reconciliation`  
**Draft pull request:** `#20`  
**Authoritative baseline:** `main` at `74a81545fcd861d99d5f2ab81aaa4c890e40d4cd`  
**Historical premium source:** `frontend/premium-visual-polish` at `8707648430c2d7d9c696c27091138aaa915fb0b0`  
**Historical requested live branch:** `lived` at `4fec4fa534fc318ac8770dbad0e3287ea1b3e589`

## Result

The complete approved premium visual-polish system remains integrated in current `main`. No premium runtime source had to be copied or rewritten.

Git ancestry proved:

```text
lived
  └── frontend/premium-visual-polish
        └── main
```

- Premium is 258 commits ahead of `lived` and 0 behind.
- Main is 100 commits ahead of premium and 0 behind.
- Main is 358 commits ahead of `lived` and 0 behind.

A literal historical merge would add no unique commit. Replacing current files with historical variants would instead regress reduced-motion safety, hydration stability, image optimization, catalogue media and later approved story work.

## Completed work

### 1. Isolated integration branch

Created:

```text
integration/main-premium-polish-reconciliation
```

The branch was created directly from current `main`. `main`, `lived` and `frontend/premium-visual-polish` were not modified.

### 2. Full reconciliation plan

Created:

```text
docs/superpowers/plans/2026-08-05-main-premium-polish-reconciliation.md
```

The plan defines dominance rules, protected boundaries, complete feature mapping, an evidence-gated repair policy, verification commands and final scope checks.

### 3. Full lineage and feature audit

Created:

```text
docs/superpowers/audits/2026-08-05-main-premium-polish-reconciliation.md
```

The audit records:

- exact branch tips and ancestry;
- the reason `main` is the dominant tree;
- protected backend, contract, security, catalogue and deployment boundaries;
- every premium motion primitive;
- every F7 stylesheet;
- public shell and mobile navigation behavior;
- homepage cinematic choreography;
- product discovery and detail motion;
- story, utility, contact and legal presentation;
- inquiry and quotation conversion motion;
- responsive, coarse-pointer, reduced-motion, hydration and performance safeguards;
- every premium-owned runtime file changed after the premium branch ended;
- successful prior closeout evidence;
- current unrelated stale admin-browser diagnostics.

The audit classified no approved premium capability as missing.

### 4. Durable cross-domain preservation gate

Created:

```text
apps/web/src/test/f7-premium-reconciliation.static.test.mjs
```

The gate contains six independent contracts covering:

1. complete Rosa-owned motion boundary;
2. premium public shell and mobile curtain;
3. homepage cinematic choreography;
4. product discovery motion and current catalogue media;
5. story, utility and legal restraint;
6. inquiry and quotation conversion polish.

The first CI execution correctly exposed one test-authoring assumption: the audit test expected `Stagger` directly in `about-page.tsx`, while the component that actually owns the buyer-list stagger is `supported-buyers.tsx`. No runtime defect existed. The assertion was moved to the owning component, then the complete verification pipeline was rerun from the beginning.

### 5. No speculative runtime rewrite

The completed matrix found every approved feature preserved or improved. Therefore:

- no motion primitive was replaced;
- no page composition was rewritten;
- no CSS effect was reintroduced from history;
- no product data or media mapping was touched;
- no backend, API, authentication or deployment file was touched.

This is the safest possible implementation of the owner's requirement: preserve all premium work without sacrificing the more advanced current branch.

## Premium-owned files changed after the historical branch

The following later changes were retained deliberately:

| File group | Current improvement retained |
|---|---|
| `Reveal`, `Stagger`, `TextReveal`, `RouteTransition` | Deterministic server/client markup; reduced-motion final state controlled through CSS; hydration divergence removed. |
| `Magnetic`, `TiltSurface` | Deterministic MotionValues with reduced-motion pointer guards. |
| `MediaFrame` | Optimized Next.js Image, responsive sizes, priority, stable focal point and overlays. |
| `globals.css` | Catalogue-media, scissors-evolution and reduced-motion closeout imports; premium stylesheet remains final. |
| About page | Approved `ScissorsEvolution` addition. |
| Family product card and gallery | Current exact catalogue media, fallback paths, sprite indexes and responsive sizes. |
| F7 tests | Updated integrated reduced-motion and catalogue-media expectations. |

Restoring historical versions would have removed these improvements.

## Fresh verification

### Verification run

GitHub Actions run:

```text
30951717347 — Temporary Premium Reconciliation Verification
```

Verified PR merge commit:

```text
b0743b3936f24a21557bc82656c976c6bffe402c
```

Source branch head under test:

```text
9318997df4638951a8a487a5104f994b4bd21f45
```

The temporary workflow was removed after evidence was recorded.

### Exact results

| Gate | Result |
|---|---|
| Frozen pnpm installation | Passed using pnpm `11.20.0` |
| Supply-chain lockfile policy | 881 entries passed |
| Reconciliation preservation gate | 6 passed, 0 failed |
| Complete static foundation suite | 44 passed, 0 failed |
| ESLint | Passed |
| Vitest files | 75 passed, 0 failed |
| Vitest tests | 344 passed, 0 failed |
| Strict TypeScript | Passed |
| Next.js production build | Passed |
| Premium Playwright matrix | 28 passed, 2 intentional skips, 0 failed |

### Browser matrix executed

```text
tests/e2e/f7-public-shell.spec.ts
tests/e2e/f7-homepage-polish.spec.ts
tests/e2e/f7-product-polish.spec.ts
tests/e2e/f7-story-pages.spec.ts
tests/e2e/f7-conversion-polish.spec.ts
tests/e2e/f7-reduced-motion.spec.ts
tests/e2e/f7-responsive-restraint.spec.ts
```

The 30 browser cases completed as:

```text
28 passed
2 intentional skips
0 failed
```

### Non-blocking warnings observed

- Next.js reports the existing middleware-file convention deprecation and recommends the future `proxy` convention.
- Motion prints informational reduced-motion messages while the reduced-motion browser cases deliberately emulate that user preference.
- GitHub Actions reports that `actions/checkout@v4` and `actions/setup-node@v4` target deprecated Node 20 internally while the workflow forces Node 24.

None of these warnings caused a test, lint, typecheck, build or browser failure.

## Separate pre-existing diagnostics issue

The repository's existing `Temporary Final Closeout Diagnostics` workflow still runs historical F3E admin browser expectations. Those expectations describe static informational admin pages, while current integrated admin routes contain live/protected forms and route behavior.

This reconciliation did not alter that unrelated workflow or admin runtime. Importantly, the fresh reconciliation run passed:

- all 44 current static foundation contracts, including current F3E policy contracts;
- all 344 Vitest tests, including current admin unit/component tests;
- lint, TypeScript and production build.

The stale browser-diagnostics maintenance remains a separate task and is not evidence of a premium regression.

## Changed files relative to main

The intended permanent branch delta is limited to:

```text
apps/web/src/test/f7-premium-reconciliation.static.test.mjs
docs/superpowers/plans/2026-08-05-main-premium-polish-reconciliation.md
docs/superpowers/audits/2026-08-05-main-premium-polish-reconciliation.md
docs/superpowers/completions/2026-08-05-main-premium-polish-reconciliation.md
README.md
```

The temporary verification workflow was created only to execute the branch against the actual repository, then deleted. It is not part of the intended final diff.

## Protected-boundary confirmation

No changes were made to:

```text
services/api/**
packages/contracts/openapi/**
packages/contracts/src/generated/**
Supabase/authentication implementation
public quotation API or persistence
catalogue product registries
catalogue media manifests or binaries
admin runtime behavior
Cloudflare/OpenNext configuration
package manifests or lockfile
main
lived
frontend/premium-visual-polish
```

## Final classification

| Domain | Result |
|---|---|
| Motion foundation | Preserved and reduced-motion/hydration hardened |
| Global shell/navigation | Preserved |
| Homepage cinematic composition | Preserved |
| Product discovery/detail | Preserved and connected to newer catalogue media |
| Story/utility/legal | Preserved with later ScissorsEvolution addition |
| Inquiry/quotation conversion | Preserved |
| Responsive/accessibility/performance restraint | Preserved and strengthened |
| Backend/security/contracts/deployment | Preserved unchanged from main |
| Runtime repairs required | None |

The integration branch now provides direct, repeatable evidence that the premium visual work was not lost and that the advanced current `main` implementation remains intact.